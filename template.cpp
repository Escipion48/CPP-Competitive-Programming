//
// Created by escip on 17/04/2026.
//

#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>
#include <set>
#include <queue>
#include <stack>
#include <cmath>
#include <iomanip>

using namespace std;

// --- Macros de Atajo ---
#define fastio ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
#define pb push_back
#define sz(x) (int)(x).size()
#define F first
#define S second

// --- Tipos de Datos ---
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pii;

// --- Constantes Comunes ---
const int INF = 1e9;
const ll LINF = 1e18;
const int MOD = 1e9 + 7;

void solve() {
    // Aquí va la lógica principal de cada problema

}

int main() {
    // Optimización de I/O
    fastio;

    int t = 1;
    // Si el problema tiene múltiples casos de prueba, descomenta la siguiente línea:
    // cin >> t;

    while(t--) {
        solve();
    }

    return 0;
}
